// Deep-link target for message notifications (/messages/<conversationId>).
// Renders the same Messages UI, which reads the conversation id from the route
// via useParams and opens that thread.
export { default } from "../page"
