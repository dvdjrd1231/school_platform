; @vercel/analytics declares optional peers for other frameworks (remix, svelte, vue).
; Auto-installing them pulls in vulnerable transitive deps this Next.js app never imports.
auto-install-peers=false
